# alx-school

## Project setup

### Node.js Server
```
cd backend
node server.js
```

### Angular Client
```
cd frontend
```
Run `ng serve --port 8081` for a dev server. Navigate to `http://localhost:8081/`.
